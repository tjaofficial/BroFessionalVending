// Tenant Dashboard Script

// Example: Preload data if needed (can fetch data dynamically if required)
document.addEventListener('DOMContentLoaded', () => {
    console.log('Tenant Dashboard Loaded');

    // Additional dynamic features can be added here
});
